#!/usr/bin/env bash
# install.command – Vivaldi AI Tab Sorter setup (macOS)
# Double-click this file in Finder to install the extension and Vivaldi bridge.

cd "$(dirname "$0")" && exec bash install.sh
